apiVersion: v1
kind: Service
metadata:
  name: devbrew-fe
  namespace: devbrew
spec:
  selector:
    app: devbrew-fe
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
