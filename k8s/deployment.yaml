apiVersion: apps/v1
kind: Deployment
metadata:
  name: devbrew-fe
  namespace: devbrew
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devbrew-fe
  template:
    metadata:
      labels:
        app: devbrew-fe
    spec:
      containers:
        - name: devbrew-fe
          image: ghcr.io/yoon6yo/devbrew-fe:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
